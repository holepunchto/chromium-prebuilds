import("//prebuilds/common.gni")

is_prebuild = "content"

# Don't use a separate data file for ICU, but embed it statically instead.
# This increases executable size, but simplifies distribution.
icu_use_data_file = false
